import { Media } from './mediaBase';

/**
 * Props para el componente MediaMetadata
 * @interface MediaMetadataProps
 * @property {Media} media - Media
 */
export interface MediaMetadataProps {
  media: Media;
}