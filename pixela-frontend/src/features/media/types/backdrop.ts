/**
 * Props para el componente BackdropImage
 * @interface BackdropImageProps
 * @property {string} backdropUrl - URL de la imagen de fondo
 */
export interface BackdropImageProps {
  backdropUrl: string;
} 