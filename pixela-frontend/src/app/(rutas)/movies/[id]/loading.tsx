import { MediaPageSkeleton } from '@/app/components/skeletons';

/**
 * Loading state para página individual de película
 */
export default function LoadingMoviePage() {
  return <MediaPageSkeleton />;
} 